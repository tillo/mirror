To: sysadmins@tails.net
Subject: New Tails mirror — tails.mdapi.ch

Hi,

I'd like to add my server to the Tails mirror pool.

  Base URL:  https://tails.mdapi.ch/tails/
  Rsync:     rsync://mirror.mdapi.ch/tails

The HTTPS vhost serves the `amnesia-archive` rsync tree at its root, so
the upstream `tails/` directory ends up one level under the hostname —
same shape as the `/pub/tails/` prefix in your Apache example, just
without the redirect.

The mirror uses a dedicated hostname per the setup guide, serves the
archive at the root of the vhost over HTTPS and HTTP (HSTS-redirected to
HTTPS), and exposes the same tree over Rsync for your pool's health
checks.

Sync command, run at minute :00 of every hour with a uniformly random
0..40 minute jitter (flock-guarded):

    rsync -rt --delete --timeout=300 \
      rsync://rsync.torproject.org/amnesia-archive/ \
      /srv/ftp/tails/

I'd prefer to pull from rsync.tails.net::amnesia-archive (your canonical
source) but that daemon rejects connections from my egress IP
(31.3.128.55) — looks allow-listed. rsync.torproject.org serves the
exact same module and is open. Happy to switch if you'd like to add me
to your allow list.

Location: Echallens, Switzerland
Bandwidth: 10 Gbps dual-stack FTTH (IPv4 + IPv6)
Operator: Martino Dell'Ambrogio <tillo@tillo.ch>
For public listing alongside the URL: tillo@tillo.ch (no alias needed)

I already operate public mirrors of BlackArch Linux and Gentoo Portage on
the same host (mirror.mdapi.ch), so the infrastructure is well-established.

Thanks,
Martino
